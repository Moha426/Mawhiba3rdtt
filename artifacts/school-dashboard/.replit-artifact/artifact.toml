kind = "web"
previewPath = "/"
title = "لوحة التحكم المدرسية"
version = "1.0.0"
id = "artifacts/school-dashboard"
router = "path"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23889

[services.development]
run = "pnpm --filter @workspace/school-dashboard run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/school-dashboard", "run", "build" ]
serve = "static"
publicDir = "artifacts/school-dashboard/dist/public"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23889"
BASE_PATH = "/"
